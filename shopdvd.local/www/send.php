<?php
print_r($_POST);
  // $name=$_POST['name'];
  // $tel=$_POST['phone'];

  // $to="smexova@bk.ru";


  // $subject= "";
  // $subject= "";
  // $massage = "
  // Имя: ".htmlspecialchars($name). "<br>
  // Телефон:".htmlspecialchars($tel);"<br>
  // Сумма:".$_POST["check"].
  // $headers = "from: Заявка с сайта www.yuristi.online   <no-reply@mysyte.ru>\r\nContent-type:text/html; charset=utf-8 \r\n";
  // mail($to, $subject, $massage, $headers);
  // header('Location: thanks.php');
  // exit;